// Make this a client page and render the client-only dashboard directly.
'use client';

import DashboardClient from './DashboardClient';

export default function DashboardPage() {
  return <DashboardClient />;
}
